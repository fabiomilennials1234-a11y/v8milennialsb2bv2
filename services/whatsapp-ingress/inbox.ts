// Shared admission must also ship inside the Supabase function bundle.
export { admitReceipt, skipDisabledGroupUpdate, type InboxEvent } from '../../supabase/functions/_shared/whatsapp-ingress-inbox.ts';
