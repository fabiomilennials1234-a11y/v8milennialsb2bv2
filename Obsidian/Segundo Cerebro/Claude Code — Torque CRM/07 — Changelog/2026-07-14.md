# 2026-07-14

## Mudanças
- **workflows / node condição**: campo UTM (`utm_campaign`/`utm_source`/`utm_medium`/`utm_content`/`utm_term`) agora usa **Combobox creatable** no input de "Valor" — lista os valores UTM que já existem nos leads da org (read-only) e ainda permite digitar valor livre. Elimina erro de acento/colchete/pontuação em valores vindos do Meta (ex.: `[TESTE CRIATIVOS] BATERIA.`).

## Arquivos tocados
- `src/modules/workflows/hooks/useOrgUtmValues.ts` — **novo** hook read-only. Query `leads.select(<field>)` filtrada por org + not-null + not-empty + limit(1000); dedup (Set) + sort `localeCompare` pt-BR no client (Postgrest não tem DISTINCT). Allowlist ESTRITA de 5 colunas UTM (`UTM_VALUE_FIELDS`) antes de interpolar a coluna no `select` (anti column-injection). org de `useOrganization` (NÃO `useAuth` — este não expõe `organizationId`).
- `src/modules/workflows/components/sidebar-panels/UtmValueCombobox.tsx` — **novo** combobox creatable (Command+Popover, padrão ProductCombobox). Estados: loading / vazio ("digite manualmente") / lista + opção `Usar "<texto>"`.
- `src/modules/workflows/components/sidebar-panels/ConditionPanel.tsx` — 3º ramo no bloco "Valor" (`isResponsibleField` → Select / `isUtmField` → combobox / else → Input). `handleFieldChange`: ao ENTRAR num campo UTM vindo de operador numérico/incompatível, default para `contains` (preserva operador de texto já sensato).
- Testes: `hooks/__tests__/useOrgUtmValues.test.ts` (dedup+sort+allowlist+enabled) e `components/__tests__/ConditionPanel.test.tsx` (render combobox, seleção/creatable→onUpdate, operador→contains).

## Decisões
- **Backend intocado** — o evaluator (`_shared/workflow-condition-evaluator.ts`) já resolve `leadData[utm_*]` (colunas reais de `leads`). Sem migration/RPC/edge function.
- Fonte única da allowlist = `UTM_VALUE_FIELDS` no hook; `ConditionPanel` reusa via import (não duplica o Set).

## Follow-ups
- Nenhum débito criado. Nota: `tsc -p tsconfig.app.json` tem baseline pré-existente (~1916 erros, não bloqueia CI que gateia em `vite build` + `test:unit`); esta mudança é net-neutral (0 erros nos arquivos tocados).

---

## Mudanças — Painel "Movimentações no período" (Performance)
- **analytics / Performance**: novo painel ADITIVO no topo de `Performance.tsx` (fora das Tabs, acima do ranking/gamificação — lógica de competição/metas intocada). Conta **marcadas / comparecidas / vendido** por **DATA DE MOVIMENTAÇÃO** (evento no ledger canônico), NÃO por data de criação do lead. Filtro de período próprio (presets Hoje/7 dias/30 dias/Mês/Custom, default Mês), isolado do Select mês/ano do ranking.

## Arquivos tocados
- `supabase/migrations/20270315000000_get_movement_metrics.sql` — **nova** RPC `get_movement_metrics(p_org_id, p_start, p_end, p_member_id default null)` → `TABLE(marcadas int, comparecidas int, vendido_count int, vendido_receita numeric)`. Lê SÓ ledgers canônicos: marcadas=`meeting_events` `meeting_booked` por `occurred_at`; comparecidas=`meeting_held` por `coalesce(meeting_date, occurred_at)`; vendido=`sale_events` `sale` por `sold_at`, LÍQUIDO de estornos (anti-join `sale_reversed`) → count + Σ `sale_value`. Sem gate de flag (ledgers populam por trigger). SECURITY DEFINER + `search_path = public, extensions` + `assert_org_access`; `REVOKE ALL FROM PUBLIC` + `GRANT EXECUTE TO authenticated, service_role`. Intervalo `[p_start,p_end]` FECHADO (casa com `end` = 23:59:59.999 do frontend). Atribuição single-key: reunião→`pre_sale_responsible_id`, venda→`sale_responsible_id`. Passa `scripts/check-metric-antipatterns.sh`.
- `src/modules/analytics/hooks/useMovimentacoesPeriodo.ts` — **novo** hook (TanStack Query v5, `queryKey ['movement-metrics', orgId, start, end, memberId]`, `enabled: !!orgId && !!start && !!end`). orgId via `useOrganization`. Normaliza `numeric` (string) → number.
- `src/modules/analytics/lib/movimentacoes-period.ts` — **novo** resolver puro de datas (presets → range UTC, mesma semântica de `utc-day`/`computeSaudePeriodRange`, estendido com janelas móveis 7d/30d) + (de)serialização do estado persistido.
- `src/modules/analytics/components/performance/MovimentacoesPanel.tsx` — **novo** painel (glass-card, 3 `MovimentacaoTile` inline com anatomia fiel ao `KPICard`: racing-stripe + chip + count-up; tile Vendido = hero com linha "Receita · R$ …" cheia). `PeriodRangeControl` (pílula de presets + Popover Calendar range 2 meses). Estados loading/vazio/erro, motion c/ `prefers-reduced-motion`, a11y (role=group + aria-label, presets aria-pressed). Estado via `usePersistedState("perf-movimentacoes-period")`.
- `src/modules/analytics/pages/Performance.tsx` — monta `<MovimentacoesPanel />` no topo do return, abaixo do header mês/ano, acima de `<Tabs>`.
- Testes: `tests/unit/movimentacoes-period.test.ts` (resolver, 11), `tests/unit/useMovimentacoesPeriodo.test.ts` (RPC args/member/numeric/disabled/erro, 6), `tests/unit/MovimentacoesPanel.test.tsx` (loading/erro+retry/zero/dados/a11y/preset, 6) — **23 passam**.

## Decisões
- RPC purpose-built em vez de reusar `get_sales_metrics`/`get_dashboard_metrics`: leitura por data-de-evento com intervalo arbitrário (não período nomeado) e sem gate `canonical_metrics`. Espelha a leitura líquida de estorno do #995.
- Painel isolado: zero toque em ranking/competição/metas/`get_dashboard_metrics`/`get_ranking_data`.

## Follow-ups
- 🔴 **Migration NÃO aplicada em dev**: MCP Supabase sem acesso ao projeto dev (`bcfadphgsibjzivtbjvc`) e `supabase db push --linked` BLOQUEADO por drift de histórico (`20270313000000` remote-only — colisão conhecida de lead_origins). CTO/arquiteto: aplicar `20270315000000` em dev via `migration repair`/`db pull` ou canal com credenciais, depois `supabase gen types` (ou manter cast `as never` do hook). Prod: só com autorização.
- Types: hook usa `supabase.rpc("get_movement_metrics" as never, ... as never)` (padrão dos hooks canônicos) até regen de types pós-apply.
