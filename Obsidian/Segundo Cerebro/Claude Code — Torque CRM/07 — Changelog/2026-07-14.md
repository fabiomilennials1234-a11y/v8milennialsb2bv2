# 2026-07-14

## Mudanças
- **workflows / node condição**: campo UTM (`utm_campaign`/`utm_source`/`utm_medium`/`utm_content`/`utm_term`) agora usa **Combobox creatable** no input de "Valor" — lista os valores UTM que já existem nos leads da org (read-only) e ainda permite digitar valor livre. Elimina erro de acento/colchete/pontuação em valores vindos do Meta (ex.: `[TESTE CRIATIVOS] BATERIA.`).

## Arquivos tocados
- `src/modules/workflows/hooks/useOrgUtmValues.ts` — **novo** hook read-only. Query `leads.select(<field>)` filtrada por org + not-null + not-empty + limit(1000); dedup (Set) + sort `localeCompare` pt-BR no client (Postgrest não tem DISTINCT). Allowlist ESTRITA de 5 colunas UTM (`UTM_VALUE_FIELDS`) antes de interpolar a coluna no `select` (anti column-injection). org de `useOrganization` (NÃO `useAuth` — este não expõe `organizationId`).
- `src/modules/workflows/components/sidebar-panels/UtmValueCombobox.tsx` — **novo** combobox creatable (Command+Popover, padrão ProductCombobox). Estados: loading / vazio ("digite manualmente") / lista + opção `Usar "<texto>"`.
- `src/modules/workflows/components/sidebar-panels/ConditionPanel.tsx` — 3º ramo no bloco "Valor" (`isResponsibleField` → Select / `isUtmField` → combobox / else → Input). `handleFieldChange`: ao ENTRAR num campo UTM vindo de operador numérico/incompatível, default para `contains` (preserva operador de texto já sensato).
- Testes: `hooks/__tests__/useOrgUtmValues.test.ts` (dedup+sort+allowlist+enabled) e `components/__tests__/ConditionPanel.test.tsx` (render combobox, seleção/creatable→onUpdate, operador→contains).

## Decisões
- **Backend intocado** — o evaluator (`_shared/workflow-condition-evaluator.ts`) já resolve `leadData[utm_*]` (colunas reais de `leads`). Sem migration/RPC/edge function.
- Fonte única da allowlist = `UTM_VALUE_FIELDS` no hook; `ConditionPanel` reusa via import (não duplica o Set).

## Follow-ups
- Nenhum débito criado. Nota: `tsc -p tsconfig.app.json` tem baseline pré-existente (~1916 erros, não bloqueia CI que gateia em `vite build` + `test:unit`); esta mudança é net-neutral (0 erros nos arquivos tocados).
