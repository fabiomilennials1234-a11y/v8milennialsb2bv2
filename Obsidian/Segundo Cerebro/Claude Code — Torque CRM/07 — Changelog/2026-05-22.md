# 2026-05-22

## TV Dashboard v2

Redesign do TV Dashboard com período rotativo global, novos blocos de performance e correção de bug crítico no funil.

### Adicionado

- **Período rotativo global** (`hoje → 7d → 2sem → mes`, cycle 12s, sincronizado)
  - `src/lib/tv-periods.ts` + testes (14)
  - `src/contexts/TVPeriodContext.tsx`
  - `src/components/tv/PeriodPill.tsx`
- **SDR Performance Block** (Pré-Vendas — Marcadas/Comparecidas/No-Show por SDR + totais)
  - Hook `src/hooks/useSDRPerformance.ts` + testes (4)
- **Closer Performance Block** (Reuniões/Propostas/Vendas/R$/Ticket/Conversão)
  - Hook `src/hooks/useCloserPerformance.ts` + testes (5)
- **Novos Leads Block** (total + sparkline diário + top sources)
  - Hook `src/hooks/useNewLeads.ts` + teste (1)
- **KPI Row como resumo do período** via `useTVKPIs(range)`
- **RotatingSlot** para alternar Coach IA ↔ Ranking
- ADR [[../04 — Decisões/ADR-2026-05-22-tv-dashboard-period-rotation]]
- Doc feature [[../06 — Features/Dashboard/TV Dashboard]]

### Corrigido

- **Bug funil "Reuniões Marcadas"** (`src/hooks/useTVDashboardData.ts`):
  - Antes: filtrava por `meeting_date` → reuniões marcadas neste mês com data futura sumiam
  - Depois: split `confirmacoesMarcadasNoMes` (por `metrics_period_at ?? created_at`) × `currentMonthConfirmacoes` (por `meeting_date`, para comparecidas/no-show)
  - Regressão coberta em `tests/unit/useTVDashboardData-funnel.test.ts` (3)

### Removido

- Bloco "Vendas do Mês" (lista) do TV. Funil + Closer block + Ranking cobrem.

### Stack

- 45 testes unitários novos/atualizados, 100% passando
- Type-check clean
- HMR validado em `localhost:8083/tv`
