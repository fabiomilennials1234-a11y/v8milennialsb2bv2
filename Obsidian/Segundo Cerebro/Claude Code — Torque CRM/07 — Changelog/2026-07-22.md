# 2026-07-22

## Mudanças
- **leads / duplicados**: página `/duplicados` estava quebrada em TODAS as orgs — o hook chamava 2 RPCs (`find_duplicate_leads`, `merge_leads`) que nunca foram escritas (mascaradas por `as any`). Escrito o backend faltante + surfaced erro real na UI. **✅ APLICADO EM PROD** (ledger `20260722184420`) — ~245 leads duplicados voltam a ser visíveis.
- **leads / duplicados (reconciliação apply live)**: o apply em prod pegou 3 bugs, corrigidos direto em prod e reconciliados no repo. Arquivo de migration **renomeado** `20270725000000_…` → `20260722184420_…` (bate com a versão do ledger, senão vira drift):
  - **[FIX-1]** `SET pg_trgm.similarity_threshold='0.6'` na função dava `permission denied` no role de migration → removido; branch de nome filtra explícito `a.name % b.name AND similarity(a.name,b.name) >= 0.6`.
  - **[FIX-2]** índice trocado do single-col `idx_leads_name_trgm` (GIN trgm puro NÃO acelera self-join `col % col`) pelo **composite btree_gin** `idx_leads_org_name_trgm (organization_id, name gin_trgm_ops) WHERE deleted_at IS NULL` → inner scan index-driven. Exige `CREATE EXTENSION btree_gin`. EXPLAIN live **3279ms→931ms** (org 1380 leads). Single-col DROPADO em prod.
  - **[FIX-3]** `REVOKE EXECUTE … FROM anon` explícito nas 2 fns — anon herda EXECUTE via ALTER DEFAULT PRIVILEGES (não via PUBLIC), então `REVOKE FROM PUBLIC` é no-op pra anon (gotcha recorrente, ver `reference_anon_execute_via_public_grant`).

## Arquivos tocados
- `supabase/migrations/20260722184420_duplicate_leads_rpcs.sql` — NOVA (renomeada de `20270725000000` p/ bater c/ ledger). Índice composite btree_gin `idx_leads_org_name_trgm (organization_id, name gin_trgm_ops)` parcial deleted_at IS NULL (+ `CREATE EXTENSION btree_gin` + `DROP INDEX IF EXISTS idx_leads_name_trgm` defensivo) + `find_duplicate_leads(p_organization_id)` (org explícita validada por assert_org_access; match phone/email/`name % name AND similarity>=0.6`; deleted_at IS NULL; a.id<b.id; DISTINCT ON) + `merge_leads(p_keep,p_merge)` (guards self/cross-org + assert_org_access IDOR + **pre-dedupe DATA-DRIVEN** via catálogo pg_constraint/pg_index p/ todo índice UNIQUE que inclui a FK→leads.id, trata parciais; re-aponta FK via information_schema; cancela workflow_executions; delete). Ambas SECURITY DEFINER, search_path pinado, REVOKE FROM PUBLIC + GRANT authenticated + REVOKE FROM anon explícito.
- `src/modules/leads/hooks/useDuplicateLeads.ts` — removidos 2 `as any` → `as never` + args/retorno tipados; `find` passa `p_organization_id`; `useMergeLeads` void + invalida chaves de kanban (pipeline-page/stage-counts/entries).
- `src/modules/leads/pages/Duplicates.tsx` — branch de erro real (`isError`) com retry, antes engolia 404 como "nenhuma duplicata".
- `src/modules/leads/hooks/useDuplicateLeads.test.ts` — NOVO (vitest, 5 casos).
- `supabase/tests/duplicate_leads_rpcs_test.sql` — NOVO (pgTAP, plan 23: grants/anon + `has_index(idx_leads_org_name_trgm)`, match, multi-tenant + IDOR, guards merge, FK re-point, pre-dedupe pipeline_entries) + registrado em `run.sh`.

## Decisões (revisão do revisor)
- Pre-dedupe do merge = DATA-DRIVEN via catálogo (não lista estática) — o schema cresceu 7 tabelas com UNIQUE(lead_id …) além das 8 originais (incl. pipeline_entries → colisão garantida no caso comum, merge abortava). Descoberta dinâmica previne recorrência.
- Match por nome = operador `%` (indexável) + índice GIN trgm, não `similarity()>=0.6` (nested-loop N² estourava timeout na maior org, 3906 leads).
- `find` recebe org explícita validada (scoping correto p/ master/multi-org, reduz blast-radius de perf).

## Follow-ups
- ✅ Migration APLICADA em prod (ledger `20260722184420`); índice composite criado CONCURRENTLY out-of-band; single-col dropado. Dev: aplicar via `db push` (replay limpo — `CREATE INDEX IF NOT EXISTS` + `DROP INDEX IF EXISTS` defensivo).
- ⏳ Regen `src/integrations/supabase/types.ts` (requer CLI) → então remover os `as never` do hook. Comando no header de `useDuplicateLeads.ts`.
- Detalhe completo: `.specs/fixes/duplicate-leads-rpcs-missing-backend.md`.

---

## Mudanças — #1209 (fura-fila, segurança)
- **permissões / métricas**: `assert_org_access` não filtrava `is_active` — **membro desativado seguia lendo receita, ranking e comissão** da org que o desativou. Em prod: **12 usuários em 5 orgs** com vínculo apenas inativo passavam no gate. Corrigido + a irmã `assert_org_member` (mesmo defeito, guarda os leitores de dinheiro da Carteira).
- **gestor de portfólio**: descoberto de quebra que `assert_org_access` nunca contemplou o ramo do gestor (ADR-0021). O único gestor ativo de prod não é `team_member` nem master das 3 orgs que gerencia → o gate **já o rejeitava**. Incluir o ramo é correção de um 2º defeito, não afrouxamento.

## Arquivos tocados
- `supabase/migrations/20270726000000_assert_org_access_require_active_member.sql` — NOVA. `assert_org_access` e `assert_org_member` passam a **delegar** a regra de vínculo a `get_my_organization_ids()` (= `team_members` ativos UNION gestor), em vez de reimplementá-la. Preserva service_role → master → membro ativo/gestor; adiciona guard de `p_org_id NULL`; REVOKE PUBLIC/anon + GRANT authenticated/service_role.
- `supabase/tests/assert_org_access_test.sql` — NOVA (pgTAP, plan 25): ativo / desativado / master / service_role / gestor + `NULL` + regressão de `get_sales_metrics` + **planted-failure** (replanta a definição antiga e prova que sob ela o desativado passava e o gestor era bloqueado).
- `supabase/tests/run.sh` — suíte registrada nos dois caminhos (pg_prove e fallback psql).
- `.specs/fixes/assert-org-access-inactive-member-leak.md` — NOVO, laudo completo.

## Decisões
- **Delegar em vez de duplicar**: o furo nasceu de `assert_org_access` e `get_my_organization_ids` reimplementarem a mesma regra e divergirem em silêncio. Delegar mata a classe do bug, não só a instância.
- **`assert_org_member` no mesmo escopo**: defeito idêntico guardando `get_portfolio_*`, `get_revenue_at_risk`, `get_vendedor_ranking`, `get_pending_orders`. Fechar só um seria trancar a porta da frente.
- **`20270211000001` (swap do gestor) ESTÁ aplicado em prod** — verificado via `pg_get_functiondef`. O cabeçalho do arquivo diz "NÃO APLICADO" e está desatualizado. Confiar no banco, não no arquivo.

## Follow-ups / BLOQUEADO
- **Nada aplicado em nenhum ambiente.** Dev (`bcfadphgsibjzivtbjvc`) está **404 migrations atrás** (436 de 840 aplicadas) e **não tem `assert_org_access`** (`20261114000011` faltando) nem a fundação do gestor → dev não serve de alvo de verificação para esta fatia. Alvo correto = Postgres local (`supabase start`, aplica as 840 do zero, igual ao CI). Travado porque o Docker Desktop parou num diálogo de senha de admin do macOS — precisa do CTO aprovar, então `supabase start && bash supabase/tests/run.sh`.
- **O furo se repete em 23 outras funções `SECURITY DEFINER`.** Pior camada: predicados de RLS — `is_user_responsible` (15 policies), `is_campanha_viewer` (9), `is_campanha_member` (5) = **29 policies** onde o desativado ainda satisfaz o predicado. Depois `resolve_org_for_rpc` (5 chamadores, resolve org com `LIMIT 1` sem `is_active`). Merece fatia própria; sugerido invariante pgTAP INV-5 ratcheted em baseline 23. Lista completa por tier no `.specs`.

### #1209 — APLICADO EM PROD (autorização explícita do CTO)
Versão gravada: **`20260722205847`** / `assert_org_access_require_active_member`. Arquivo do repo é `20270726000000` — **drift conhecido**, mesma mudança sob dois identificadores. Não reaplicar achando que falta.

**Por que prod e não outro lugar**: não existe ambiente de validação hoje. Docker travado em diálogo de senha do macOS; dev aposentado (404 migrations atrás, sem a função); branch efêmera impossível (`MIGRATIONS_FAILED` desde 2026-03-11 — repo não replaya, morre em jan/2026). Rollback capturado via `pg_get_functiondef` e **testado executando** antes de qualquer escrita.

**Baseline em prod ANTES da escrita** — furo provado ao vivo: desativado PASSOU nos dois gates; gestor BLOQUEADO nas 3 orgs que gerencia.
**Depois**: 14/14 no gate + 8/8 nos leitores. Zero falhas, rollback não acionado.

**CORREÇÃO ao item B da revisão** (o número do revisor estava errado, não pode ir pro PR): não são "2 ativos cortados". Dos 12 pares, **3 são masters e NÃO são cortados** (`is_master_user()` avaliado antes do ramo de vínculo) — são justamente os 3 logins mais recentes, o que criou a ilusão. Sobram **9 não-masters**, login mais recente 2026-06-02, 2 nunca logaram, nenhuma org órfã. Razão de fundo: `team_members.is_active` é **assento pago**, não acesso — `org_resolve_quota`/`max_users` conta ativos e desconta master de propósito, então staff da casa fica inativo e entra pelo caminho master.

**Falso alarme registrado**: os 5 leitores da Carteira têm `EXCEPTION WHEN OTHERS ... IF SQLERRM = 'access_denied' THEN RETURN NULL` — o gate dispara e vira payload vazio por design. Parecia leitura na primeira passada; não é. Asserido conteúdo: desativado recebe NULL / `total=0,rows=0` / `[]` nos cinco. Lição: **ausência de exceção não é prova de leitura**.

**Nits do revisor aplicados**: `assert_org_access_test.sql` devolve `session_replication_role = origin` após as fixtures; cabeçalho de `20270211000001_gestor_portfolio_helper_union_swap.sql` corrigido de "⛔ NÃO APLICADO" para "✅ APLICADO", que era o estado real e induzia erro em quem lesse.

**Segue fora de escopo**: os 23 leitores restantes com o mesmo padrão e o ratchet INV-5.
