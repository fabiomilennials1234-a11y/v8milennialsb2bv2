---
date: 2026-04-24
branch: multiple
agents: [Conductor, Backend, DBA, Frontend, Infra, Security, AI]
tags: [changelog, bugfix, custom-pipelines, import-leads, copilot, uazapi]
---

# 2026-04-24 — diagnóstico "Uazapi prod errors" + hotfix generate_message loop

## Pedido inicial

CTO: "integração Uazapi não subiu corretamente, muitos erros de edge function — testar org Milennials".

## Diagnóstico (via harness — Conductor + multi-agente)

**Premissa CTO incorreta**. Erros prod **não são Uazapi**. Migração Uazapi nem chegou em data plane.

### Erros REAIS (Milennials, runtime_logs últimas 2h)

| Erro | Frequência | Causa |
|---|---|---|
| `copilot retry:generate_message — Tipo desconhecido` | ~1/min | [workflow-executor.ts:344](../../../../supabase/functions/_shared/workflow-executor.ts:344) enfileira `generate_message`, [ai-action-executor.ts](../../../../supabase/functions/_shared/ai-action-executor.ts) sem case → loop infinito retry+cron pickup. Débito desde commit `799a563` (2026-03-13). |
| `Evolution sendText/sdr HTTP 500: Connection Closed` | bursts | Instância "sdr" Milennials `disconnected` — última conexão 17:17 UTC. Workflows continuam disparando. |

### Estado migração Uazapi (confirmado)

- 0/52 instâncias com `provider="uazapi"` (todas Evolution Baileys)
- `whatsapp_instance_secrets` rows = 0
- Secrets prod **AUSENTES**: `UAZAPI_BASE_URL`, `UAZAPI_ADMIN_TOKEN`, `UAZAPI_WEBHOOK_SECRET`
- `whatsapp_provider_override` NULL em todas orgs
- Edge functions Uazapi DEPLOYADAS hoje 23:50 UTC (`whatsapp-webhook v3`, `whatsapp-api-proxy v2`, `history-sync-worker v1`, `mass-send-* v2`, `agent-message v111`)

### Bombas-relógio Uazapi (não exploderam ainda)

- [whatsapp-api-proxy/index.ts:174](../../../../supabase/functions/whatsapp-api-proxy/index.ts:174) hardcoda `provider: "uazapi"` em createInstance → primeiro user que clicar "Conectar WhatsApp" novo recebe `UAZAPI_BASE_URL not set`
- [whatsapp-webhook/index.ts:490](../../../../supabase/functions/whatsapp-webhook/index.ts:490) `timingSafeEqual(secret, "")` rejeita 401 todo webhook Uazapi
- Frontend `MigrationBanner` + `RepairingWizard` visíveis mas nada migra

### Status fix-copilot-fallback (entrada do diagnóstico)

`agent-message v111` deploy `2026-04-23 23:50 UTC` — bridge Uazapi→agent OK em prod. Esta parte funciona.

## Ações executadas

### P0 — sangramento parado

1. **REST PATCH prod**: 2 `pending_ai_actions` Milennials em loop → `status='dead_letter'`
   - IDs: `cf17df67-4fe2-441c-b469-1e4667a4f4f1`, `f3728398-9703-4cbd-92e4-b60a71d5f43c`
   - 1 row já estava `dead_letter` desde 2026-04-14
2. **Hotfix branch** `hotfix/ai-action-executor-generate-message-noop`:
   - Case `generate_message` → no-op success + warn log
   - 4 testes ativos (regressão guard)
   - tsc 0 erros, vitest 4/4 pass + 37 adjacentes pass
   - Commit local (sem push)

### P1 — backlog criado

[workflow-copilot-pipeline-design](../../08%20—%20Backlog/em-progresso/workflow-copilot-pipeline-design.md) — design rota correta workflow node `copilot` → agent outbound dispatch.

## Recomendações pendentes (não executadas — aguardar OK CTO)

1. **Reconectar instância "sdr" Milennials** (UI Evolution manual) ou pausar workflows ativos
2. **Setar 3 secrets Uazapi prod** antes de qualquer org tentar provisionar
3. **Feature flag** `ENABLE_UAZAPI_PROVISIONING` em `whatsapp-api-proxy:174` — default false até migração progressiva real
4. **Esconder/desabilitar UI** `MigrationBanner` + "Conectar nova instância Uazapi" enquanto secrets ausentes
5. **Sprint dedicado** migração progressiva: 1 instância piloto não-Milennials → smoke test → rollout 1 org/dia

## Lição

`generate_message` ficou enfileirando há ~6 semanas sem alarme efetivo. Cron retries silenciosamente se acumulam quando handler não existe. Adicionar:
- Alarm em `pending_ai_actions` com `retry_count >= 3 AND status='pending'` por > 1h
- Default branch do switch deve `Sentry.captureException` antes de retornar erro

## Update — execução pós-aprovação CTO

### Hotfix #1 deployed (generate_message no-op)

Push direct main FF: `b0f0cd4..1a2942e`. Deploy `process-ai-actions v34` + `agent-message v113` (2026-04-24 00:44 UTC).

Validação prova objetiva runtime_logs Milennials:
- `00:43:03 copilot retry:generate_message error "Tipo desconhecido"` (handler antigo)
- `00:46:04 copilot generate_message success` (handler novo)
- `00:46:56 copilot generate_message success` (handler novo)

2 stuck pending → `completed`.

### Hotfix #2 deployed — whatsapp-api-proxy default Evolution

CTO confirmou: manter Evolution todas orgs até secrets Uazapi resolvidos. Código Uazapi preservado.

Branch `hotfix/whatsapp-api-proxy-evolution-default`. Patch [whatsapp-api-proxy/index.ts:165-180](../../../../supabase/functions/whatsapp-api-proxy/index.ts:165): provisioning gate via env flag `ENABLE_UAZAPI_PROVISIONING` + check de UAZAPI_BASE_URL/ADMIN_TOKEN. Default Evolution.

Push direct main FF: `7a5ad2c..d0747c0`. Deploy `whatsapp-api-proxy` (versão nova).

### Issue secundário (operacional)

3/3 instâncias WhatsApp Milennials `disconnected`:

| Instance | Last connection |
|---|---|
| sdr | 2026-04-23 17:17 UTC |
| numero1 | 2026-04-07 |
| gabriel | 2026-03-23 |

Workflows registram `WhatsApp instance not available`. Requer reconexão manual (QR re-scan via UI Evolution).

### Status final prod

| Item | Status |
|---|---|
| generate_message loop | ✓ PARADO (handler ativo, validado) |
| Chat Evolution | ✓ Funcional para orgs com instância conectada |
| whatsapp-api-proxy createInstance | ✓ Default Evolution (Uazapi atrás de flag) |
| Uazapi código | ✓ Preservado (gate via env flag) |
| Secrets Uazapi prod | ⏳ pendente CTO obter valores |
| Instâncias Milennials | ⚠️ 3/3 disconnected — reconexão manual CTO |

## Referências

- Conductor triagem: harness multi-agente
- Hotfix branch: `hotfix/ai-action-executor-generate-message-noop`
- Backlog P1: [workflow-copilot-pipeline-design](../../08%20—%20Backlog/em-progresso/workflow-copilot-pipeline-design.md)
- Diagnóstico Uazapi anterior: [2026-04-23](2026-04-23.md)

---

# 2026-04-24 (tarde) — Fix import leads em Custom Pipelines

## Bug reportado

Usuários clicavam em "Importar" dentro de funis customizados, UI mostrava sucesso, mas a lista não entrava no funil. Funis padrões (`pipe_whatsapp`, `pipe_confirmacao`, `pipe_propostas`) funcionavam normal.

## Root cause

`custom_pipe_entries.assigned_to` referenciava `profiles(id)` — **única tabela de pipeline no sistema assim**. Todas outras (`pipe_whatsapp`, `pipe_confirmacao`, `pipe_propostas`, `pipe_distribution`, `campanhas`, `upsell`) referenciam `team_members(id)`.

Cadeia da falha:
1. Frontend enviava `team_members.id` via `useTeamMembers()` → `memberOptions`
2. Edge function `importToCustomPipeline` repassava direto pra `assigned_to`
3. Postgres rejeitava com FK violation (team_members.id ≠ profiles.id)
4. INSERT/UPDATE em `custom_pipe_entries` não capturava `error` nem usava `.select().single()` — falha silenciosa
5. `report.created++` já tinha rodado na linha 997 (insert em `leads` funcionou), UI reportava sucesso
6. Funil vazio

Bug era intermitente: se usuário escolhesse "Sem atribuição" e CSV não tivesse coluna `seller_name`, `assigned_to = null` → FK aceitava null → lead entrava. Qualquer atribuição quebrava.

Master virtual (`master-virtual-<uuid>`) também quebrava (UUID inválido).

## Fix aplicado (3 camadas)

### 1. Migration (`supabase/migrations/20260918000000_fix_custom_pipe_entries_assigned_to_fk.sql`)

- Converte rows existentes: `profiles.id → team_members.id` via match por `user_id` + `organization_id` (preserva multi-tenancy)
- Usa `DISTINCT ON (cpe.id) ORDER BY is_active DESC, updated_at DESC` quando múltiplos team_members ativos por profile/org
- Nullifica órfãos sem match
- DROP FK antiga (`profiles`) + ADD nova (`team_members` ON DELETE SET NULL)
- Idempotente via DO blocks
- `RAISE NOTICE` com count de rows convertidas/nullificadas (audit)

### 2. Edge function (`supabase/functions/import-leads/index.ts`)

- Captura `error` no INSERT/UPDATE de `custom_pipe_entries` + `.select().single()`
- Se falha e lead era novo: decrementa `report.created`, incrementa `report.rejected`, push em `report.errors`
- `console.error` com context (leadId, orgId, pipelineId, stageId, assignedTo) — sem PII
- `truncateErr(msg, 200)` evita vazar mensagens longas do Postgres

### 3. Frontend (`src/components/custom-pipelines/ImportCustomPipelineContent.tsx`)

- `memberOptions` filtra `isVirtualTeamMember(m.id)` + `m.is_active !== false`
- Mensagem pós-import condicional: se `lastReport.errors.length > 0`, exibe quantos entraram + aponta pros erros

### 4. Testes (`tests/integration/lead-import.test.ts`)

Regression suite cobrindo:
- INSERT em `custom_pipe_entries` com `team_members.id` deve funcionar pós-migration
- UUID inválido deve retornar FK violation observável (não silenciosa)
- `assigned_to = null` deve funcionar (responsável opcional)

## Security gate

Security aprovou com condições (todas aplicadas):
- Migration: `RAISE NOTICE` com count + uso de `DISTINCT ON` pra multi-match determinístico
- Edge function: `truncateErr` limita vazamento de error.message
- Deploy order: **migration antes da edge function**, staging antes de prod

## Deploy plan

1. Backup: `pg_dump --table=custom_pipe_entries` antes de aplicar em prod
2. Staging (`bcfadphgsibjzivtbjvc`): migration → edge function → smoke test import custom pipeline
3. Prod (`jsjsmuncfkbsbzqzqhfq`): migration → edge function → frontend deploy

Rollback: DROP nova FK + re-ADD antiga + restore do dump.

## Arquivos tocados

- `supabase/migrations/20260918000000_fix_custom_pipe_entries_assigned_to_fk.sql` (novo)
- `supabase/functions/import-leads/index.ts` (patch `importToCustomPipeline` + helper `truncateErr`)
- `src/components/custom-pipelines/ImportCustomPipelineContent.tsx` (filtro virtual + msg final)
- `tests/integration/lead-import.test.ts` (3 tests novos)
