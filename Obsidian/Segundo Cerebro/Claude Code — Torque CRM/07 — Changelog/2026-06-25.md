# 2026-06-25

## Mudanças
- **crm-mcp (C2 backend)**: implementada a infra de PAT do MCP customer-facing (cenário B, ADR 0012) — tabela `personal_access_tokens` + RLS + trigger de imutabilidade + RPC resolver hermética; auth per-PAT → client RLS-scoped per-user (ES256 self-mint, nunca master/service_role); tracer tool `lead.get` customer-scoped; espinha `_shared/mcp/` estendida com `customerExposed`/`toolFilter` fail-closed (não-breaking p/ torque-mcp). Read-only duro. Branch `feat/crm-mcp/c2-pat-infra` (não aplicado/deployado).
- **crm-mcp (C2 UI)**: edge fn `create-pat` (gera token via `crm-mcp/lib/pat.ts`, grava só o hash, retorna plaintext 1×; INSERT sob RLS do user; master-reject) + `PersonalAccessTokensPanel` na aba "Tokens IA" de Configuracoes (criar/listar/revogar, display-once com snippet de config MCP p/ Claude Desktop, aviso role-aware p/ admin) + hooks `usePersonalAccessTokens`. Branch `feat/crm-mcp/c2-ui-pat` (stacked em #889). Build ✓, tsc 0 erros, 4 unit. ⚠️ Coexiste com `api_keys` (REST org-level) — coerência de produto a decidir pelo CTO.

## Arquivos tocados
- `supabase/migrations/20270101000000_crm_mcp_personal_access_tokens.sql` — tabela + RLS (owner/admin/master-ghost) + trigger imutável + `crm_mcp_resolve_token` (hermética, dobra is_master+current_org_ids, executável por anon)
- `supabase/functions/crm-mcp/index.ts` — L1+L2 wiring (parse→hash→resolve→classify→eligibility→mint→assertWellFormedJwt→user client→dispatch)
- `supabase/functions/crm-mcp/lib/{pat,config,phone}.ts` (+ `.test.ts`) — token format/hash, mint ES256, guards, classifiers; assert-absent de secrets de ops; normalize PT em TS (anti-bypass)
- `supabase/functions/crm-mcp/tools/{lead,index}.ts` (+ `lead.test.ts`) — tracer `lead.get` (org do token, `.eq` DiD) + allowlist `CUSTOMER_TOOLS`
- `supabase/functions/_shared/mcp/{types,registry,dispatch}.ts` (+ `registry.test.ts`) — `ToolDef.customerExposed?`, `ToolContext.orgId/userId/scopes?`, `visibleTools` toolFilter, `DispatchContext.toolFilter?`, log label por server
- `supabase/functions/deno.json` — import map `jose@5.9.6`
- `supabase/config.toml` — `[functions.crm-mcp] verify_jwt=false`
- `tests/integration/crm-mcp-pat.test.ts` — anchor do resolver + RLS da tabela (CI)
- `docs/adr/0012-crm-mcp-customer-facing-pat.md` — decisão formal
- `.specs/features/crm-mcp/DESIGN.md` — §7.6 reconciliado (resolver anon-callable hermético) + status C2 backend

## Decisões
- ADR 0012 — crm-mcp customer-facing, PAT per-user, RLS-puro; mint ES256 dedicado; PAT de master recusado no resolve; resolver hermético executável por anon (resolve o dilema §7.6).

## Follow-ups
- Resto da C2: UI de PAT (criar/listar/revogar, display-once, aviso role-aware) + endpoint/flow de criação (gera token + grava hash).
- Aplicar migration em **dev**; setar secrets `CRM_MCP_JWT_SIGNING_KEY`/`CRM_MCP_JWT_KID` + confiar a chave pública ES256 no JWKS do projeto; canary round-trip no deploy.
- Re-confirmar `is_master_user unpinned=0` em prod antes do ship (guard, já pinada).
- C3 read pack; C4 rate limit; C5 lifecycle/auditoria/offboarding-revoke.
