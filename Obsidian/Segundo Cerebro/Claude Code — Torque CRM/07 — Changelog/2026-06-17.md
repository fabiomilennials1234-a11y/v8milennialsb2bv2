# 2026-06-17

## Mudanças
- **pipelines/leads (ghost-stage classe, Camada 4)**: guard de etapa ativa no
  chokepoint de INSERT das views `pipe_*`. Lead criado via path não-coberto pela
  Camada 1 (RPC `create_lead_with_pipe` ← `webhook-new-lead`/`webhook-confirmacao`,
  e writers frontend das views) não cai mais em stage inativo quando a org tem
  etapa ativa. Path culpado da "Flávia" (org Dna de Almas): `webhook-new-lead` →
  RPC → view `pipe_whatsapp` gravava `'novo'` literal sem checar `is_active`.

## Arquivos tocados
- `supabase/migrations/20261220000000_ghost_stage_guard_pipe_insert.sql` — novo
  helper `fn_resolve_active_stage_key` + `pipe_*_insert_fn` passam a resolver stage ativo.
- `src/modules/communication/hooks/useWhatsAppLeadIntegration.ts` — `useLinkLeadToWhatsApp`
  resolve 1ª etapa ativa (era `status:'novo'` hardcoded).
- `src/modules/pipelines/components/kanban/CreateOpportunityModal.tsx` — resolve
  1ª etapa ativa via `usePipelineStages('whatsapp')` (era `status:'novo'` hardcoded).
- `tests/integration/ghost-stage-guard-pipe-insert.test.ts` — 5 cenários (novo).
- `Obsidian/.../07 — Changelog/2026-06-09-ghost-stage-ingest-guard.md` — Camada 4 anexada.

## Decisões
- Fix na INSTEAD OF INSERT trigger das views (chokepoint único de TODO writer de
  pipe canônico) em vez de patchar N call-sites — uma fonte da verdade, cobre RPC +
  frontend + edge de uma vez. Frontend ajustado também (defense-in-depth + intenção).
- Fallback estático das views preservado verbatim — guard só adiciona a resolução
  de etapa ativa na frente; comportamento de org não-seedada inalterado.

## Follow-ups
- DEPLOY pendente (autorização CTO): edge fns que chamam a RPC herdam o fix via DB,
  mas a **migration precisa ser aplicada em prod** p/ valer; frontend via EasyPanel.
- A org DNA já foi corrigida manualmente em prod nesta sessão (fora deste código).
- CI guard de colisão de migration está RED em main (15 dups vs baseline 13) —
  PRÉ-EXISTENTE (ver `project_migration_version_collisions.md`), não introduzido aqui.
  Minha migration `20261220000000` é prefixo único.
