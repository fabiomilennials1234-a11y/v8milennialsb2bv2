# 2026-07-09

## Mudanças

- **DB / Auditoria**: auditoria adversarial completa do banco de prod (velocidade/efetividade/erros/dups) — 35 agentes, 24 achados confirmados, 5 refutados. Reframe central: os "gargalos" do dossiê (chat 3s, kanban) são **fósseis** do `pg_stat_statements` cumulativo (nunca resetado); único write pesado vivo = `INSERT whatsapp_messages` 30,8ms. Roadmap reestruturado em 5 ondas por ROI.
- **DB / Segurança (Onda 0)**: implementados os artefatos de migration dos slices 0.1 (revoke fns de retenção de mídia executáveis por anon) e 0.2 (bucket `media`/`help-media` público listável → PII enumerável sem login). Runbook de rotação do `CRON_SECRET` (0.3). **Não aplicado em prod** — gated em "vai" do CTO; dev bloqueado (402).

## Arquivos tocados

- `supabase/migrations/20270309000000_revoke_media_retention_fns.sql` — REVOKE EXECUTE FROM anon, authenticated nas 2 fns (`list_expired_whatsapp_media`, `invoke_whatsapp_media_retention`) + `LEAST(p_limit, 5000)`. Fecha ADV-1.
- `supabase/migrations/20270309000001_media_bucket_authenticated_read.sql` — substitui policy `"Allow public read"`/`"help_media_read"` por SELECT `TO authenticated`. Fecha ADV-2 (enumeração sem login). getPublicUrl segue 200.
- `Obsidian/.../05 — How-to/rotacionar-cron-secret.md` — runbook manual (2 lugares: secret edge fn + `cron_config.cron_secret`).
- `.specs/features/db-optimization/SPEC.md` + `tasks.md` — spec de execução das 5 ondas.
- `Obsidian/.../02 — Arquitetura/Dossiê DB — Saúde e Roadmap.md` — §5 Roadmap revisado + reframe do fóssil.

## Decisões

- **Não editar as migrations imutáveis `20270303000000/001`** (regra 1 de `migrations/CLAUDE.md`) — a nova `20270309000000` roda por último no replay → estado ACL final correto sem mutar migration que já rodou.
- **Gotcha registrado:** `REVOKE FROM PUBLIC` é no-op para grant via `pg_default_acl` (explícito a anon/authenticated) — inspecionar `proacl` antes de escolher o grantee. Aplicar em specs futuras de hardening.
- **Ordem por ROI, não por "acelera o chat"** — o chat lento era fóssil (delta de 2 snapshots pgss prova).

## Follow-ups

- **Gate de prod (aguarda CTO):** aplicar 0.1 + 0.2 em prod (`jsjsmuncfkbsbzqzqhfq`) via `db push` / Mgmt API + rodar as VERIFY queries embutidas. 0.2 exige o gate pré-apply (confirmar que `"Allow public read"` é media-scoped em `pg_policies`).
- **3 decisões bloqueantes** (§6 do Dossiê): arranque Onda 0, NatuPlast (Slice 1.1), visibilidade de chat (gate Onda 4 → ADR).
- Onda 1 (operacional: DLQ veneno + gate liveness workflows) na fila.
- Hardening definitivo do default: `ALTER DEFAULT PRIVILEGES ... REVOKE EXECUTE ... FROM anon, authenticated` (comentado na 0.1; ship como change própria — mata a raiz do ADV-1/Tier B).
