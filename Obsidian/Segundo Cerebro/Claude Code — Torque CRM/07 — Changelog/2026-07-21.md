# 2026-07-21

## Mudanças
- **WhatsApp anti-ban / Workflows — Send Governor (Onda 1)**: novo choke único `governOutboundSend()` / `commitOutboundSend()` / `governGate()` em `_shared/action-handlers/send-governor.ts`, plugado em TODOS os handlers de envio de automação (workflow). Substitui o throttle fixo de 3s por 4 alavancas atrás de sub-flag `shadow→enforce` + kill-switch mestre por org. **Defer-first: NUNCA falha um envio** — quando uma trava de volume/horário dispara, reagenda o nó do workflow (`next_run_at`) em vez de erro. **Propriedade inviolável: flag OFF = no-op byte-idêntico** (mesmo throttle 3s, sem cap, sem quiet-hours, sem defer) — provado por teste. Alavancas Onda 1: jitter `[3000,8000)ms`, cap por-número (80/dia), cap org-wide (200/dia), quiet-hours `08:00–20:00 Seg–Sáb BRT` + morning-spread. Orçamento COMPARTILHADO com Mass Send (mesma linha de ledger — intencional).
- **Reconciliação Onda 0**: jitter REUSA `anti-ban-jitter.ts` (range default delega a `jitterDelayMs`) → envio de workflow espaça igual aos workers de massa/cron. `reputation-signal.ts` já está no transporte (`uazapi-client.ts`) abaixo do envio governado → governor NÃO re-loga sinal de ban (evita double-count). Os handlers de workflow em main não tinham jitter/reputation (Onda 0 só tocou massa/cron), então o governor é o 1º controle anti-ban nesse path — sem sobreposição a remover.
- **Fix GAP #6**: `send_to_number` (hand-off pro vendedor) não tinha throttle NENHUM — agora gated como `operator_notification` (jitter+cap, isento de quiet-hours pra não estourar SLA do lead).
- **Fix gap do draft stale**: `sendWhatsAppDocument` (4º handler em `send-whatsapp-media.ts`) foi gated+commitado também (o draft anterior só cobria audio/image/sticker).

## Arquivos tocados
- `supabase/functions/_shared/action-handlers/send-governor.ts` — **novo** (~470 linhas). Choke + config resolution + guards de defer + commit dos ledgers + `governGate` de conveniência.
- `supabase/migrations/20270320000000_workflow_send_governor.sql` — **novo**. Aditiva: 2 colunas em `organizations` (`workflow_send_governor_enabled boolean NOT NULL DEFAULT false` + `workflow_send_governor jsonb`). Sem função nova → sem grant surface, sem mudança de RLS.
- `supabase/functions/_shared/action-handlers/whatsapp-helpers.ts` — split `enforceWhatsAppRateLimit` → extrai `getLastOutgoingAt()` + param `intervalMs` (default 3000, retrocompat byte-idêntico).
- `supabase/functions/_shared/action-handlers/types.ts` + `workflow-action-handler.ts` — `ActionResult.deferUntil`, `ActionContext.nodeId`, `_nodeId` em `toActionInput`, e skip do `lead_history` quando `deferUntil` (msg não foi enviada).
- `supabase/functions/_shared/workflow-executor.ts` — case `action` honra `deferUntil` (espelha pausa do `time_window`: mesmo `current_node_id`, `next_run_at`, status `running`, sem avançar grafo/retry/fail) + `_governor_deferred` na skip-list de loop-detection.
- `send-whatsapp.ts`, `send-whatsapp-media.ts` (×4: audio/image/sticker/document), `send-whatsapp-rich.ts` (×3: template/menu/pix), `send-campaign-message.ts`, `send-to-number.ts`, `send-meta.ts` — `governGate()` no ponto do antigo `enforceWhatsAppRateLimit`; `gate.commit()` 1× por msg entregue (nunca no dedup-duplicate).
- `tests/unit/action-handlers/send-governor.test.ts` — **novo** (helpers puros + orquestrador + commit + reuso jitter Onda 0). `tests/unit/action-handlers/send-whatsapp.test.ts` + `tests/unit/workflow-executor.test.ts` — casos de governor/defer adicionados.
- `.specs/features/whatsapp-antiban/onda-1-workflow-governor.md` — **novo** (design + slices + decisões travadas + handoff de deploy DEV).

## Decisões
- **Defer-first, nunca fail.** Incidente `wait_business_window` (2026-07-21) deixou leads presos em `processing`. Guards inegociáveis: (1) `deferUntil > now+60s` sempre; (2) teto `_defer_counts[nodeId]` = 5 ⇒ **FAIL-OPEN** (envia) + log terminal; (3) erro de leitura de ledger ⇒ defer CURTO (5min), `resolveGovernorConfig` cai pra DISABLED em erro (blip nunca liga governor nem congela orgs); (4) `_governor_deferred` pula loop-detection; (5) `claim_workflow_executions` filtra `next_run_at<=now` (verificado).
- **Segurança**: governor roda service_role; org SEMPRE do contexto de execução (nunca body); `instance_id` via `getWhatsAppInstance` (org-scoped); `usage_date` sempre `saoPauloUsageDate` (BRT); increment RPCs já service_role-only (intocados). Sem PII em shadow-logs (só ids/contadores/hash). Migration sem função ⇒ sem grant surface.
- **Onda 2 (próximo PR, NÃO neste)**: warm-up ramp (d1=8→≥d5=cap, nunca reseta) + cold-lead gate (soft→escala, consumindo `reputation_ban_signal`). Shape de alavanca já preparado pra estender sem reformar o choke.

## Auto-QA
- `vitest` nos 3 arquivos tocados: **60 passed**.
- `deno check` nos arquivos novos/editados: **0 erros novos** (baseline do grafo = 56 erros idêntica em HEAD pristina — todos em arquivos intocados: `workflow-condition-evaluator.ts`, case `wait_business_window`/`BehaviorWindow`, `sanitizeOutput`). `send-governor.ts` limpo em isolamento.
- `eslint`: **0 erros** (20 warnings `no-explicit-any` pré-existentes, nenhum em linha nova).

## Follow-ups
- **Deploy DEV pendente (handoff)** — DEV tem 5 migrations não-aplicadas (`20270314`–`20270319`) e MCP sem permissão; **NÃO** `db push`. Aplicar SÓ `20270320000000` + `supabase functions deploy process-workflow-executions --project-ref bcfadphgsibjzivtbjvc` + UPDATE org Milennials-dev (`6030520a-...`) com alavancas em `shadow`. Validar via `runtime_logs` (module whatsapp, action `workflow_send_governor`). Detalhe no spec.
- Onda 2 (warm-up + cold-gate) permanece aberta.
