# 2026-06-08

## Mudanças
- **WhatsApp / disparo**: vínculo responsável→instância (`user_write_instance_strict`) vira **preferência, não gate**. Quando o lead não tem responsável (ou responsável sem instância / inativa), o disparo cai para a 1ª instância CONECTADA da org em vez de falhar. Corrige "disparo só funciona com responsável" (flag estava ON em PROD para Milennials).

## Arquivos tocados
- `supabase/functions/_shared/whatsapp-dispatch.ts` — `resolveDispatchContext`: soft codes (`NO_RESPONSIBLE`/`NO_INSTANCE`/`INSTANCE_INACTIVE`) → fallback `resolveInstance(requireConnected:true)`; só `LEAD_NOT_FOUND` propaga.
- `supabase/functions/_shared/instance-write-guard.ts` — `resolveStrictInstanceForCaller`: soft codes → `null` (caller usa legado); `LEAD_NOT_FOUND` → throw. Cabeçalhos atualizados ("NÃO faz fallback" → faz).
- `tests/integration/instance-write-guard.test.ts` — +11 testes (27 total): fallback por soft code, preferência mantida, `LEAD_NOT_FOUND`, org sem instância conectada → `no_instance`.
- `.specs/features/whatsapp-write-instance/feature-overview.md` — §13 (hotfix + correção do §11 desatualizado).

## Decisões
- Vínculo per-user vira preferência (fallback connected). Trade-off LGPD aceito pelo CTO — `04 — Decisões/ADR-2026-06-08-disparo-fallback-instancia-conectada.md`.

## Follow-ups
- Confirmar em PROD que o quick-blast manual (`quick-blast-create`) não tem gate de responsável em função deployada (código de `main` é limpo). Se ainda travar, é função deployada desatualizada — redeploy.
- Revisar o plano de cutover do `whatsapp-write-instance`: §11 do feature-overview está desatualizado (PROD já tem a migration + flag ON em Milennials).
- Sem deploy aplicado nesta sessão (default dev). Deploy das 2 edge functions exige autorização do CTO.

## Validação (counts literais)
- `vitest run tests/integration/instance-write-guard.test.ts` → **27 passed (27)**.
- `vitest run tests/unit/action-handlers tests/unit/shared-action-handler*.test.ts` → **295 passed (18 files)**.
- `eslint` (3 arquivos tocados) → **0 errors, 30 warnings** (todos `no-explicit-any`, padrão pré-existente).
- `npm run build` → **✓ built** (frontend intacto).
