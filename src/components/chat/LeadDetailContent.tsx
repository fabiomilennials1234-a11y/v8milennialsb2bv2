/**
 * LeadDetailContent — stub de re-export (backwards-compat).
 *
 * Onda 3.1, C10: path canônico invertido para src/components/lead/LeadDetailContent.tsx.
 * Este arquivo mantém compatibilidade com WhatsAppChat e LeadContactModal
 * que importam via "./LeadDetailContent" sem alterar uma linha nesses consumidores.
 *
 * Migration de path para Onda 3.2 (opcional): atualizar imports e remover este stub.
 */
export {
  LeadDetailContent,
  type LeadDetailContentProps,
} from "@/components/lead/LeadDetailContent";
