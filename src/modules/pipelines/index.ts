/**
 * Module pipelines — API pública.
 *
 * Public surface do bounded context. Tudo cross-module passa por aqui.
 * Internals (subpastas) são privados — ESLint `boundaries` impede import direto.
 *
 * 🚧 Vazio — exports virão durante a slice 5 (`feat/modularizacao/04-pipelines`).
 * Ver `./CLAUDE.md` para escopo (pipe_whatsapp/confirmacao/propostas + custom),
 * entidade primária (Pipeline + Stage + Pipeline Entry).
 */

export {};
