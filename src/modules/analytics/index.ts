/**
 * Module analytics — API pública.
 *
 * Public surface do bounded context. Tudo cross-module passa por aqui.
 * Internals (subpastas) são privados — ESLint `boundaries` impede import direto.
 *
 * 🚧 Vazio — exports virão durante a slice 12 (`feat/modularizacao/11-analytics`).
 * Ver `./CLAUDE.md` para escopo, entidade primária, áreas frágeis.
 */

export {};
