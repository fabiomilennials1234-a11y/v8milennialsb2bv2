/**
 * Module engagement — API pública.
 *
 * Public surface do bounded context. Tudo cross-module passa por aqui.
 * Internals (subpastas) são privados — ESLint `boundaries` impede import direto.
 *
 * 🚧 Vazio — exports virão durante a slice 11 (`feat/modularizacao/10-engagement`).
 * Ver `./CLAUDE.md` para escopo (checklist, activities, agenda, gamification),
 * entidade primária (Checklist + Activity + Follow-up).
 */

export {};
