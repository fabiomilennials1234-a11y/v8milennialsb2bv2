/**
 * Module communication — API pública.
 *
 * Public surface do bounded context. Tudo cross-module passa por aqui.
 * Internals (subpastas) são privados — ESLint `boundaries` impede import direto.
 *
 * 🚧 Vazio — exports virão durante a slice 6 (`feat/modularizacao/05-communication`).
 * Ver `./CLAUDE.md` para escopo (WhatsApp/Meta/SZ.Chat + área frágil Uazapi),
 * entidade primária (Conversation + Message + Instance), e áreas frágeis.
 */

export {};
