/**
 * Module platform — API pública.
 *
 * Public surface do bounded context. Tudo cross-module passa por aqui.
 * Internals (subpastas) são privados — ESLint `boundaries` impede import direto.
 *
 * 🚧 Vazio — exports virão durante a slice 14 (`feat/modularizacao/13-platform`).
 * Ver `./CLAUDE.md` para escopo (Onboarding + Settings + Observability + Health
 * + Dead Letter).
 */

export {};
