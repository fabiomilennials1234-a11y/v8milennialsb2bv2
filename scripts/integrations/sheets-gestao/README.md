# Planilha GESTÃO — automação de escrita (página 2)

Scripts da automação da planilha **GESTÃO** (Google Sheets / Apps Script + builders Python).
Relocados da raiz do repo em 2026-06-30 (eram `_page2_*` / `_gestao_*` scratch não-versionado).

| Arquivo | O que é |
|---|---|
| `pagina2-escrever.gs` | Google Apps Script — escreve a página 2 (briefing de gestão) na planilha. |
| `page2-briefing.py` | Builder Python do conteúdo de briefing da página 2. |
| `page2-build.py` | Builder Python da estrutura/layout da página 2. |

Origem: auditoria de funil da planilha GESTÃO (jun/2026). Mantido versionado por ser
fonte das automações de relatório, não derivável do resto do repo.
