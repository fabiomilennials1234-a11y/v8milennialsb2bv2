/**
 * Trilha 3.B Fase B1 / T3B.7c — Followup (skeleton)
 *
 * Status: SKELETON — agendamento de follow-up.
 *
 * Funções alvo:
 *   - generateFollowupMessage (linha 3076) — gera mensagem follow-up via LLM
 *   - getNextSendTime (já em _shared/followupSchedule.ts — re-export)
 *
 * Estimativa: 2h.
 */

export { getNextSendTime } from "../followupSchedule.ts";

// TODO: extrair generateFollowupMessage de agent-engine.ts
