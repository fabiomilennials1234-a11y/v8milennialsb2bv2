/**
 * Stub for virtual:pwa-register in test environment.
 * Tests vi.mock() this module to control SW behavior.
 */
export function registerSW(_opts?: Record<string, unknown>): (reloadPage?: boolean) => Promise<void> {
  return async () => {};
}
